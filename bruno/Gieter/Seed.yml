info:
  name: Seed
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5173/api/seed
  auth:
    type: bearer
    token: seed-secret

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
